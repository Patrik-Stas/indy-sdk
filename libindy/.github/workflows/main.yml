name: CI

on: [push]

jobs:
  say-hello-world:
    runs-on: ubuntu-16.04
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - name: Branch name
        run: echo running on branch ${GITHUB_REF##*/}
      - name: Say hello
        run: |
          echo "hello world"